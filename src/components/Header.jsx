import icon from "/readily-icon.png";

export const Header = () => {
    return (
        <header>
            <img src={icon} className="logo" alt="readily icon" />
            <h1>Readily</h1>
        </header>
    )
}